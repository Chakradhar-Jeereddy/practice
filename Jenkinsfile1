@Library('shared-pipeline') _ 

// /vars/nodeJSEKSPipeline
def mymap = [
     project: 'roboshop',
     component: 'catalogue',
     acc_id: '406682759639'
]

if( env.BRANCH_NAME.equalsIgnoreCase('main') ){
      echo "Please fallow the CR process"
}
else{
        nodejspipeline(mymap)
}